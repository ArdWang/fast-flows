/// FlowLogic - Base logic class
library;

import 'flows_controller.dart';

/// Base class for logic objects
abstract class FlowLogic extends FlowController {
  // Extended logic class with no additional overrides
}
